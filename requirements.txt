# NOTE:
# This requirements file installs the CPU version of PyTorch by default.
# The PyTorch CPU wheel index is required for the +cpu package builds.
# If you want to use GPU acceleration, install your own compatible PyTorch GPU version
# for your CUDA/runtime setup after installing these requirements.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.8.0+cpu
torchvision==0.23.0+cpu
torchaudio==2.8.0+cpu

fastapi==0.135.3
uvicorn==0.44.0
sqlmodel==0.0.38
pydantic==2.13.0
python-multipart==0.0.26
platformdirs==4.5.0


faiss-cpu==1.13.2
numpy==2.4.3
pillow==12.1.1

transformers==5.5.0
huggingface-hub==1.9.1
timm==1.0.26
ultralytics==8.4.37

httpx==0.28.1

scikit-learn==1.8.0
